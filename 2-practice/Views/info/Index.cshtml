
@{
    ViewBag.Title = "Index";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">


<h2>@DateTime.Now.ToShortDateString()</h2>
<br />

@{
    int x = 5;
    int y = 10;
    int z = 20;
    int sum = x + y + z;
    int mul = x * y * z;

    @:Sum= @sum ;
    @:mul= @mul ;
}
<br />
<br />
@if (sum > 50)
{
    <textarea> sum greater than 50</textarea>
    @:sum greater than 50

}
else
{

    <textarea> sum less than 50</textarea>
    @:sum less than 50
}
<br />
<br />
@for (int i = 0; i < 10; i++)
{

    <text>@i </text>

}

<table class="table ">

    <tr class="table-dark">
        <td>num</td>
        <td>num^2</td>
        <td>num*3</td>
    </tr>

    @for (int i = 1; i <= 30; i++)
    {
        <tr>
            <td>@i</td>
            <td>@(i*i)</td>
            <td>@(i*3)</td>
        </tr>
    }

</table>